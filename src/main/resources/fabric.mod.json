{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "name": "${mod_name}",
  "version": "${mod_version}",
  "environment": "*",
  "entrypoints": {
    "main": [
      "com.bh.testmod.BhTestMod"
    ]
  },
  "mixins": [
    "bh-test-mod.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${fabric_loader_version}"
  }
}
